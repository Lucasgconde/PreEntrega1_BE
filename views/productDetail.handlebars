<h2>{{product.title}}</h2>

<p><strong>Descripción:</strong> {{product.description}}</p>
<p><strong>Precio:</strong> ${{product.price}}</p>
<p><strong>Categoría:</strong> {{product.category}}</p>
<p><strong>Stock:</strong> {{product.stock}}</p>

<form method="POST" action="/api/carts/6826a229795d39a142f5f962/products/{{product._id}}?_method=PUT">
  <label for="quantity">Cantidad:</label>
  <input type="number" name="quantity" id="quantity" value="1" min="1" max="{{product.stock}}" required>
  <button type="submit" style="margin-left: 10px;">Agregar al carrito</button>
</form>

<a href="/carts/6826a229795d39a142f5f962" style="display: inline-block; margin-top: 15px; color: #007bff;">
  Ver carrito
</a>
<br>
<a href="/products" style="color: #555;">← Volver al listado</a>